import assert from"node:assert/strict";import fs from"node:fs";const bank=JSON.parse(fs.readFileSync(new URL("../data/question-banks/ateez-true-fan.json",import.meta.url),"utf8"));const questions=bank.questions.filter(q=>q.verificationStatus==="verified"),quota={easy:5,medium:6,hard:4};
function hash(seed){let v=2166136261;for(const c of seed)v=Math.imul(v^c.charCodeAt(0),16777619);return v>>>0;}function rng(seed){let s=hash(seed)||1;return()=>{s+=0x6d2b79f5;let v=s;v=Math.imul(v^v>>>15,v|1);v^=v+Math.imul(v^v>>>7,v|61);return((v^v>>>14)>>>0)/4294967296;};}function shuffle(items,seed){const r=rng(seed),a=[...items];for(let i=a.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;}function album(q){return q.sourceIds[0]?.replace(/^SRC_/,"").replace(/_(SPOTIFY|KQ|REPORT|OVERVIEW)$/g,"")??null;}function valid(items,maxCategory=4){const categories=new Map(),albums=new Map();for(const q of items){categories.set(q.category,(categories.get(q.category)??0)+1);const key=album(q);if(key)albums.set(key,(albums.get(key)??0)+1);}return Math.max(...categories.values())<=maxCategory&&[...albums.values()].every(n=>n<=3)&&items.filter(q=>q.freshness==="time_sensitive").length<=3;}function session(seed){for(let attempt=0;attempt<150;attempt++){const picked=Object.entries(quota).flatMap(([d,n])=>shuffle(questions.filter(q=>q.difficulty===d),`${seed}:${attempt}:${d}`).slice(0,n));if(!valid(picked,attempt<100?4:5))continue;return shuffle(picked,`${seed}:questions`).map(q=>{const order=shuffle([0,1,2,3],`${seed}:${q.id}:options`);return{...q,order,shuffled:order.map(i=>q.options[i]),shuffledAnswer:order.indexOf(q.answerIndex)};});}throw new Error(`ATEEZ session failed:${seed}`);}
assert.equal(questions.length,60);assert.equal(new Set(questions.map(q=>q.id)).size,60);for(const d of Object.keys(quota))assert.equal(questions.filter(q=>q.difficulty===d).length,20);const sources=new Map(bank.sources.map(s=>[s.id,s]));for(const q of questions){assert.equal(q.options.length,4,q.id);assert.equal(new Set(q.options).size,4,q.id);assert.ok(q.answerIndex>=0&&q.answerIndex<=3,q.id);assert.equal(q.options[q.answerIndex],q.answerText,q.id);assert.ok(q.explanation,q.id);for(const id of q.sourceIds)assert.ok(sources.get(id)?.url,`${q.id}:${id}`);}for(let score=0;score<=15;score++)assert.equal(bank.resultBands.filter(g=>score>=g.minScore&&score<=g.maxScore).length,1,`${score}점`);for(const score of[0,3,4,7,8,10,11,13,14,15])assert.equal(bank.resultBands.filter(g=>score>=g.minScore&&score<=g.maxScore).length,1);
const positions=[0,0,0,0],fingerprints=new Set();for(let run=0;run<1000;run++){const selected=session(`ateez-${run}`);fingerprints.add(selected.map(q=>q.id).join(","));assert.equal(selected.length,15);assert.equal(new Set(selected.map(q=>q.id)).size,15);for(const[d,n]of Object.entries(quota))assert.equal(selected.filter(q=>q.difficulty===d).length,n);assert.ok(valid(selected,5));for(const q of selected){assert.equal(q.shuffled[q.shuffledAnswer],q.answerText);positions[q.shuffledAnswer]++;}}assert.deepEqual(session("stable").map(q=>[q.id,q.order]),session("stable").map(q=>[q.id,q.order]));assert.notDeepEqual(session("new-a").map(q=>q.id),session("new-b").map(q=>q.id));assert.ok(Math.max(...positions)/Math.min(...positions)<1.15);console.log(JSON.stringify({questions:60,difficulty:{easy:20,medium:20,hard:20},simulations:1000,uniqueSessions:fingerprints.size,answerPositions:positions,maxScore:15,status:"PASS"},null,2));
