"use client";import{useEffect,useRef,useState}from"react";import{useRouter}from"next/navigation";
const track=(event:string,params?:Record<string,string>)=>(window as Window&{gtag?:(command:string,event:string,params?:Record<string,unknown>)=>void}).gtag?.("event",event,params);
export function SearchForm({initialQuery}:{initialQuery:string}){const router=useRouter(),[value,setValue]=useState(initialQuery),first=useRef(true),input=useRef<HTMLInputElement>(null);useEffect(()=>{input.current?.focus();track("search_open")},[]);useEffect(()=>{if(first.current){first.current=false;return}const timer=window.setTimeout(()=>{const normalized=value.trim().replace(/\s+/g," ");router.replace(normalized?`/search?q=${encodeURIComponent(normalized)}`:"/search",{scroll:false})},300);return()=>clearTimeout(timer)},[router,value]);return <form action="/search" method="get" role="search" onSubmit={()=>track("search_submit",{query:value.trim()})} className="mt-7 flex gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-card"><label htmlFor="test-search" className="sr-only">테스트 검색어</label><input ref={input} id="test-search" name="q" type="search" value={value} onChange={event=>setValue(event.target.value)} onKeyDown={event=>{if(event.key==="Escape"){setValue("");event.currentTarget.blur()}}} placeholder="예: NCT DREAM, 엔시티 드림, 시즈니" autoComplete="off" className="min-h-12 min-w-0 flex-1 rounded-xl border-0 bg-slate-50 px-4 text-base text-ink outline-none placeholder:text-slate-400 focus:ring-2 focus:ring-primary"/>{value&&<button type="button" onClick={()=>{setValue("");input.current?.focus()}} aria-label="검색어 지우기" className="min-h-12 shrink-0 rounded-xl px-3 text-sm font-bold text-slate-500 hover:bg-slate-100">지우기</button>}<button type="submit" className="min-h-12 shrink-0 rounded-xl bg-primary px-4 text-sm font-black text-white transition hover:bg-blue-700 sm:px-5">검색</button></form>}
